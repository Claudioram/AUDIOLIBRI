name: Deploy to OPENCLAW (DISABLED)

# DEPLOY DISATTIVATO — il server OPENCLAW (135.181.32.65)
# ospita il backend di openclaw.ai. Serve una strategia che
# NON tocchi nginx/Envoy esistenti e usi una porta dedicata.
#
# Riabilitare solo dopo aver pianificato la coesistenza con
# l'infrastruttura esistente del sito.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Abort
        run: |
          echo "Deploy disattivato per evitare conflitti con openclaw.ai backend"
          exit 0
